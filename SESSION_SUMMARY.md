# Лог сессии: Навигация по линейным реперам (GPS-denied)

**Дата:** 2026-08-17
**Проект:** `/home/alex/aerial-nav/`
**Цель:** Навигация дрона (CUAV X7+ Pro) по линейным реперам через сиамскую сеть в условиях отсутствия GPS.

---

## 1. Архитектура системы

1. **Маршрут:** Линейные объекты (дороги, опушки, берега). 6 точек в долине Днепра.
2. **Карта:** Спутниковые снимки ESRI World Imagery, скачиваются узкой полосой вдоль маршрута.
3. **Навигация:** Сиамская сеть сравнивает кадр с камеры (nadir) с вырезкой из карты.
4. **Аппаратура:** CUAV X7+ Pro (автопилот), OpenIPC MC800S-V3 (камера, Sony IMX415).
5. **Параметры полёта:** Высота 1000-1200 м, скорость 80-120 км/ч, GSD ~0.4 м/пиксель.

**Маршрут (6 точек):**
```
46.344208, 32.353548
46.358646, 32.332859
46.361671, 32.181069
46.491890, 32.079251
46.668248, 31.952009
46.848488, 31.990962
```

---

## 2. Найденные и исправленные баги

### Критический баг: Перепутаны lat/lon в `route_strip_map.py`
- **Место:** Строки 236-237 и 306-307.
- **Суть:** В функции `_latlon_to_tile(lat, lon, zoom)` аргументы передавались как `(min_lon, max_lat)` — **lon и lat перепутаны**.
- **Следствие:** 
  1. Тайлы скачивались для неправильной местности (Ирак вместо Украины).
  2. При склейке координаты не совпадали → тайлы ложились мимо → PNG почти полностью серый (100,100,100).
  3. TIFF из него — тоже серый. Обучение на нём бессмысленно.
- **Исправление:** Поменяли аргументы на `(max_lat, min_lon)` и `(min_lat, max_lon)` в обоих местах.

### Ошибка OOM при обучении
- **Исправление:** Уменьшен batch size с 64 до 48.

### Ошибка Pickle в DataLoader
- **Исправление:** Добавлен `multiprocessing.set_start_method('fork')` для работы workers с rasterio.

### Ошибка UnboundLocalError
- **Исправление:** Исправлена опечатка `camera_emb = model(camera_emb)` -> `camera_batch`.

---

## 3. Созданные и изменённые скрипты

### `route_strip_map.py` (изменён)
- Исправлен баг lat/lon.
- Скачивает тайлы из ESRI с прогресс-баром.
- Сохраняет кэш в `map_cache/strips/`.

### `download_map.sh` (новый)
- Обёртка для скачивания карты.
- Автоматически удаляет старый кэш тайлов (неправильная местность).
- Поддерживает импорт маршрута из JSON: `./download_map.sh --route-file flight_mission.json`.

### `route_editor.py` (новый)
- Интерактивный редактор маршрута.
- Команды: `add`, `ins`, `set`, `del`, `yandex <ссылка>`, `name`, `save`, `load`.
- Импорт из Яндекс.Карт: поддерживает `rtext`, `ll`, `pt`, простые координаты.
- Все 6 тестов парсера пройдены.

### `stitch_map.py` (новый)
- Склейка тайлов из кэша в GeoTIFF.
- Использует реальные координаты тайлов (из имён файлов) для точного позиционирования.
- Записывает напрямую в TIFF через rasterio — не создаёт промежуточные массивы в RAM.
- Сжатие LZW.

### `convert_map_to_tiff.py` (новый, не используется)
- Альтернативный конвертер PNG→TIFF по тайлам.

### `train_route_final.py` (существующий)
- Обучение сиамской сети.
- Batch=48, workers=4, fork method.

---

## 4. Текущий статус (на 2026-08-17)

- ✅ Карта скачана: 24 597 тайлов (правильная местность).
- 🔄 Склейка тайлов в TIFF: запущено `stitch_map.py`.
- ⏳ Конвертация в TIFF (если stitch_map завершится успешно).
- ⏳ Перегенерация координат (`positive_coords.npy`).
- ⏳ Перезапуск обучения.

---

## 5. Известные ограничения

- **ESRI rate limiting:** Сервер агрессивно ограничивает частоту запросов. При HTTP 429 скрипт ждёт 5–40 секунд. Это замедляет скачивание.
- **Память:** `stitch_map.py` не загружает всё изображение в RAM, но каждый тайл обрабатывается отдельно. Для 24K тайлов это занимает время.
- **Разрешение:** 0.5 м/пиксель (zoom 19).

---

## 6. Следующие шаги

1. Дождаться завершения `stitch_map.py`.
2. Проверить TIFF: `gdalinfo -stats map_cache/antiuav_route_strip.tif | grep -E "Minimum|Maximum|Mean"`.
3. Перегенерировать координаты: `python3 generate_coords.py --map map_cache/antiuav_route_strip.tif --output training_data/route_dataset`.
4. Запустить обучение: `python3 train_route_final.py`.

---

*Лог ведётся автоматически. Последнее обновление: 2026-08-17*
