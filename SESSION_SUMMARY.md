# Лог сессий: Навигация по линейным реперам (GPS-denied)

**Проект:** `/home/alex/aerial-nav/`
**Цель:** Навигация дрона (CUAV X7+ Pro) по линейным реперам через сиамскую сеть в условиях отсутствия GPS.
**Репозиторий:** https://github.com/sviridov-aleksandr/aerial-nav

---

## Сессия 1: 2026-08-17

### Архитектура системы

1. **Маршрут:** Линейные объекты (дороги, опушки, берега). 6 точек в долине Днепра.
2. **Карта:** Спутниковые снимки ESRI World Imagery, скачиваются узкой полосой вдоль маршрута.
3. **Навигация:** Сиамская сеть сравнивает кадр с камеры (nadir) с вырезкой из карты.
4. **Аппаратура:** CUAV X7+ Pro (автопилот), OpenIPC MC800S-V3 (камера, Sony IMX415).
5. **Параметры полёта:** Высота 1000-1200 м, скорость 80-120 км/ч, GSD ~0.4 м/пиксель.

### Найденные и исправленные баги

**Критический баг: Перепутаны lat/lon в `route_strip_map.py`**
- В функции `_latlon_to_tile(lat, lon, zoom)` аргументы передавались как `(min_lon, max_lat)`.
- Следствие: тайлы скачивались для неправильной местности (Ирак вместо Украины).
- Исправление: поменяли аргументы на `(max_lat, min_lon)` и `(min_lat, max_lon)`.

**OOM при обучении:** batch size уменьшен с 64 → 48 → 16.

**Pickle в DataLoader:** добавлен `multiprocessing.set_start_method('fork')`.

**UnboundLocalError:** исправлена опечатка `camera_emb = model(camera_emb)` → `camera_batch`.

### Созданные скрипты

- `route_strip_map.py` — скачивание тайлов вдоль маршрута (исправлен lat/lon).
- `stitch_map.py` — склейка тайлов в GeoTIFF (rasterio, прямая запись, LZW/DEFLATE).
- `route_editor.py` — CLI-редактор маршрута, импорт из Яндекс.Карт.
- `train_route_final.py` — обучение сиамской сети (batch=16, workers=4, fork).

### Статус на конец сессии

- ✅ Карта скачана: 24 597 тайлов.
- ✅ Склейка в TIFF: 154880×278784 px, 627 МБ, DEFLATE.
- ✅ Обучение: 10 эпох, Loss 0.1453→0.1316, Acc 0.569.
- ❌ Тест навигации провален: Top-1=12%, медиана 50 км.
- 🔍 Диагностика: 86% тайлов — однородная вода (mean=205, std=6.1), cosine similarity=1.000.

---

## Сессия 2: 2026-08-18

### Анализ причин провала теста

Выявлены три корневые проблемы:

1. **Прямые отрезки маршрута** — 6 точек → маршрут по прямой через однородные поля, а не вдоль дорог.
2. **Узкая полоса карты** — 500 м при полёте на 1 км высоты недостаточно.
3. **Однородные тайлы** — 86% карты вода/поля, модель не может их различить.

### Решение: маршрутизация по дорогам (OSRM)

Добавлена маршрутизация через OSRM API в веб-интерфейс:
- Кликаются ключевые точки на карте → маршрут строится по дорожной сети.
- Выбор профиля: авто / пешком / вело.
- Оранжевые маркеры — waypoints, красная линия — детальный маршрут.
- Ширина полосы увеличена до 750 м (под полёт на 1 км высоты).

### Очистка диска

Удалено ~5.8 ГБ старых данных:
- `map_cache/strips` (470 МБ), `map_cache/highres` (3.1 ГБ) — старые тайлы Каланчака.
- `map_cache/antiuav_route_strip.tif` (599 МБ), `.png` (78 МБ) — битый TIFF.
- `training_data/camera_tiles.npy`, `map_tiles.npy`, `offsets.npy` (1.4 ГБ) — старые датасеты.
- `siamese_model_kalanchak.pth`, `siamese_model_kalanchak_v2_backup.pth` — старые модели.

### Новый маршрут

- 15 точек, 14.5 км, проложен по дорогам через OSRM.
- Долина Днепра, г. Каланчак (46.26-46.28N, 33.25-33.38E).
- Ширина полосы 750 м.

### КРИТИЧЕСКОЕ ОТКРЫТИЕ: ESRI vs Google

**ESRI World Imagery на zoom 19 в районе Каланчака отдаёт тайлы низкого качества:**
- Размер: 2521 байт (против 6317 у Google).
- mean=204.7, std=5.4, 79 уникальных значений — однородный фон.
- Все 8585 тайлов одинаковые — модель не может их различить.

**Google Satellite даёт реальные снимки:**
- mean=73.3, std=21.7, 148 уникальных значений.
- Дороги, застройка, растительность — реальные детали.

**Вывод:** ESRI был корневой причиной провала всех предыдущих тестов. Модель обучалась на однородном фоне и физически не могла различить тайлы.

### Перескачивание карты через Google

- Старый кэш ESRI удалён.
- Запущено скачивание через Google Satellite (тот же маршрут, 750 м полоса).
- Тайлы проверены: std=14-22, 84-148 уникальных значений — реальное качество.

### Публикация на GitHub

- Создан репозиторий: https://github.com/sviridov-aleksandr/aerial-nav
- `.gitignore`: исключены модели (*.pth), карты (*.tif), датасеты, логи, индексы.
- `README.md`: полное описание от идеи до реализации, архитектура, оборудование, workflow.
- `SESSION_SUMMARY.md`: данный лог.

### Обновление конфигурации камеры

Отказ от 30x зума в пользу OpenIPC MC800S-V3 + объектив ASX-0116 KH:
- `drone_config.py`: камера OpenIPC MC800S-V3, Sony IMX415, 4K (3840×2160), FOV 90°, без зума.
- `camera_calculator.py`: полностью переписан — убран 30x зум, добавлен расчёт по FOV 90°.
- Высота полёта: 700-1200 м (крейсерская 1000 м).
- GSD ~0.4 м/пиксель при 1000 м.
- Скорость: 15-33 м/с (54-120 км/ч).

### Улучшение веб-интерфейса

- Прогресс-бар по каждой точке/сегменту маршрута при скачивании.
- `PYTHONUNBUFFERED=1` для реального вывода лога.
- Парсинг этапов: Точка 1, Точка 2, Сегмент 1... с процентами и статусом ✓.

### План на следующую сессию

1. [IN PROGRESS] Дождаться скачивания Google-тайлов.
2. [TODO] Склеить в GeoTIFF (`stitch_map.py`).
3. [TODO] Сгенерировать координаты с фильтром однородных (`generate_coords.py --min-std 10`).
4. [TODO] Обучить модель на качественных данных.
5. [TODO] Протестировать навигацию (`test_route_dnepr.py`).
6. [TODO] Отозвать PAT-токен GitHub (безопасность).

---

## Сессия 3: 2026-08-18 (вечер)

### Run4: обучение с новым curriculum (15 эпох)

- Архитектура: torchvision ResNet-18 (ImageNet pretrained) → projection head → L2-norm. TripletLoss margin=1.0.
- Curriculum: этап 0 (эпохи 1-5) масштаб 0.25-2.0 → этап 1 (эпохи 6-10) + поворот ±30° → этап 2 (эпохи 11-15) + перспектива/погода/шум.
- Поворот без чёрных краёв: reflect-padding → rotate → вырезка центра (0% чёрных пикселей).
- Масштаб честный: `_read_scaled_patch()` читает патч scale×512 из GeoTIFF и ресемплит в 512.
- Динамика Acc: эпоха 1: 68.6% → 5: 93.1% → 8: 95.0% → 10: 96.1% → 11: 96.3% (идёт).
- ~13.8 мин/эпоху, 2196 шагов, batch=32, ~2.65 it/s.

### Тест после 10 эпох (flight_simulation_test.py, CPU, 400 тайлов индекса, 20 тестовых)

**Масштаб (взлёт):** 400м→100%, 450м→90%, 350м→85%, 300м→70%, 700м→25%, 200м→30%, 100м→15%.
**Поворот (на 400м):** 0°→100%, 5-10°→90%, 15°→65%, 20°→60%, 30°→40%, 45°→25%, 90°/180°→0%.

⚠️ **Регресс поворотов**: после 6 эпох 30°→60%, после 10 эпох 30°→40%. Acc на обучении растёт (96.1%), но обобщение на повороты ухудшилось — вероятно переобучение к тренировочным углам/масштабам этапа 1. Ждём финальный тест после 15 эпох (этап 2).

### Схема интеграции с полётным контроллером (набросок)

- Аппаратно: CUAV X7+ Pro (FC) ↔ UART/MAVLink2 921600 бод ↔ Jetson Orin Nano (компаньон) ↔ OpenIPC MC800S-V3 (камера, RTSP).
- Программно: pymavlink → MAVLink-мост → EKFNavigator (state [x,y,vx,vy,heading], predict IMU 5 Гц, update map matching 5 Гц) → Siamese matcher → координаты.

### mavlink_bridge.py (реализован)

Полный мост FC ↔ навигационный пайплайн:
- `MAVLinkBridge`: UART (921600 бод) / UDP (SITL 127.0.0.1:14540), фоновый поток телеметрии (ATTITUDE, VFR_HUD, GLOBAL_POSITION_INT, HEARTBEAT, SYS_STATUS), отправка `VISION_POSITION_ESTIMATE` с ковариацией из EKF.
- `NavigationPipeline`: камера (RTSP OpenIPC / `--sim-camera` для SITL) → вырезка 512×512 → компенсация рыскания (reflect-padding) → LocalMatcher (локальный поиск ±50 м) → EKF predict+correct → отправка позиции в FC.
- Инициализация из GPS: `GLOBAL_POSITION_INT` → `_gps_to_map_px()` (плоская проекция, origin 46.34N 31.95E).
- Зависимость: `pymavlink 2.4.49`, `pyserial 3.5` установлены в venv.
- Запуск: `--sitl --sim-camera` (тест) или `--device /dev/ttyTHS1` (реальное железо).

### Подключение реального контроллера (USB)

- Подключен CUAV X7+ Pro по USB: два порта `/dev/tyACM0` и `/dev/tyACM1` (USB ID 1209:5740 CUAV-X7-bdshot).
- Оба отвечают heartbeat: **ArduPilot (autopilot=3), тип 1 = Fixed Wing**.
- Добавлен автодетект порта в `mavlink_bridge.py` (`_auto_detect_port()`): поиск /dev/tyACM*, проверка heartbeat + телеметрии, выбор лучшего (score).
- Тест моста с реальным FC: 312 сообщений за 10 сек (~31 msg/s), телеметрия ATTITUDE/VFR_HUD/GPS работает, GPS нет в помещении (норма).
- Baud для USB CDC: 115200 (не 921600 — это для UART-телеметрии).

### Статус Run4 (на момент обновления)

- Эпоха 13/15 (56%), Acc 97.1%, этап 2 (полный набор искажений).
- После 15 эпох: финальный flight_simulation_test → train_dynamics → train_route → test_route_dnepr.

---

*Лог ведётся автоматически. Последнее обновление: 2026-08-18*